function trailingOfZeroes(N) {

    // if (n == 0) return 1;

    // let fact = 1;

    // for (let idx = 1; idx <= n; idx++) {
    //     fact = fact * idx;
    // }

    // console.log(fact);

    // let zeroes = 0;

    // while (fact % 10 == 0) {
    //     fact = fact / 10;
    //     zeroes++;
    // }
    
}

const result = trailingOfZeroes(32);
console.log(result);